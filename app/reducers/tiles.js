//import { ADD_CAR } from '../actions';

// TODO no action for the moment

export default function tiles(state = [], action) {
  switch (action.type) {
  //case ADD_CAR:
  //  return [...state, action.payload];
  default:
    return state;
  }
}
